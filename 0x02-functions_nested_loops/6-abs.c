#include "main.h"
/**
 * _abs - computes the ansolute value of an integer
 *
 * Return: 0 on success
 */
int _abs(int number)
{
	if (number < 0)
	{
		return (-number);
	}
	else
	{
		return (number);
	}
}
