#include "main.h"
/**
 * _abs - computes the ansolute value of an integer
 *
 * @n: output
 *
 * Return: 0 on success
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
